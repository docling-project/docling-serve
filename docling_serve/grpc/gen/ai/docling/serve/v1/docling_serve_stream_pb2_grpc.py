# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc
import warnings

from ai.docling.serve.v1 import docling_serve_stream_pb2 as ai_dot_docling_dot_serve_dot_v1_dot_docling__serve__stream__pb2

GRPC_GENERATED_VERSION = '1.81.1'
GRPC_VERSION = grpc.__version__
_version_not_supported = False

try:
    from grpc._utilities import first_version_is_lower
    _version_not_supported = first_version_is_lower(GRPC_VERSION, GRPC_GENERATED_VERSION)
except ImportError:
    _version_not_supported = True

if _version_not_supported:
    raise RuntimeError(
        f'The grpc package installed is at version {GRPC_VERSION},'
        + ' but the generated code in ai/docling/serve/v1/docling_serve_stream_pb2_grpc.py depends on'
        + f' grpcio>={GRPC_GENERATED_VERSION}.'
        + f' Please upgrade your grpc module to grpcio>={GRPC_GENERATED_VERSION}'
        + f' or downgrade your generated code using grpcio-tools<={GRPC_VERSION}.'
    )


class DoclingStreamingServiceStub:
    """============================================================================
    Document streaming contract (fork-owned)
    ============================================================================

    This surface is intentionally gRPC-native. It is not a 1:1 REST mirror.
    Upstream REST remains submit+poll / WebSocket for async status; this stream
    is the push channel for live progress and (eventually) incremental document
    parts.

    Sync story: every document payload type is imported from
    ai/docling/core/v1/docling_document.proto. When docling-core's Pydantic model
    gains a field, regenerate the core proto and this file stays valid — the
    envelope only wraps core messages, it does not duplicate them.

    Emission honesty:
    Phase 1 (implemented): status + source_result + final_document
    Phase 2 (orchestrator hooks): per-source fan-out as documents complete
    Phase 3 (pipeline hooks): DocumentNode parts as items hydrate
    Do not emit DocumentNode until the converter can produce partial items.
    Faking page yields from a completed DoclingDocument is not streaming.

    StreamDocument converts sources and streams StreamDocumentResponse events
    (the DocumentStreamEnvelope) until finals are emitted and the stream closes.
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.StreamDocument = channel.unary_stream(
                '/ai.docling.serve.v1.DoclingStreamingService/StreamDocument',
                request_serializer=ai_dot_docling_dot_serve_dot_v1_dot_docling__serve__stream__pb2.StreamDocumentRequest.SerializeToString,
                response_deserializer=ai_dot_docling_dot_serve_dot_v1_dot_docling__serve__stream__pb2.StreamDocumentResponse.FromString,
                _registered_method=True)


class DoclingStreamingServiceServicer:
    """============================================================================
    Document streaming contract (fork-owned)
    ============================================================================

    This surface is intentionally gRPC-native. It is not a 1:1 REST mirror.
    Upstream REST remains submit+poll / WebSocket for async status; this stream
    is the push channel for live progress and (eventually) incremental document
    parts.

    Sync story: every document payload type is imported from
    ai/docling/core/v1/docling_document.proto. When docling-core's Pydantic model
    gains a field, regenerate the core proto and this file stays valid — the
    envelope only wraps core messages, it does not duplicate them.

    Emission honesty:
    Phase 1 (implemented): status + source_result + final_document
    Phase 2 (orchestrator hooks): per-source fan-out as documents complete
    Phase 3 (pipeline hooks): DocumentNode parts as items hydrate
    Do not emit DocumentNode until the converter can produce partial items.
    Faking page yields from a completed DoclingDocument is not streaming.

    StreamDocument converts sources and streams StreamDocumentResponse events
    (the DocumentStreamEnvelope) until finals are emitted and the stream closes.
    """

    def StreamDocument(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_DoclingStreamingServiceServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'StreamDocument': grpc.unary_stream_rpc_method_handler(
                    servicer.StreamDocument,
                    request_deserializer=ai_dot_docling_dot_serve_dot_v1_dot_docling__serve__stream__pb2.StreamDocumentRequest.FromString,
                    response_serializer=ai_dot_docling_dot_serve_dot_v1_dot_docling__serve__stream__pb2.StreamDocumentResponse.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'ai.docling.serve.v1.DoclingStreamingService', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('ai.docling.serve.v1.DoclingStreamingService', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class DoclingStreamingService:
    """============================================================================
    Document streaming contract (fork-owned)
    ============================================================================

    This surface is intentionally gRPC-native. It is not a 1:1 REST mirror.
    Upstream REST remains submit+poll / WebSocket for async status; this stream
    is the push channel for live progress and (eventually) incremental document
    parts.

    Sync story: every document payload type is imported from
    ai/docling/core/v1/docling_document.proto. When docling-core's Pydantic model
    gains a field, regenerate the core proto and this file stays valid — the
    envelope only wraps core messages, it does not duplicate them.

    Emission honesty:
    Phase 1 (implemented): status + source_result + final_document
    Phase 2 (orchestrator hooks): per-source fan-out as documents complete
    Phase 3 (pipeline hooks): DocumentNode parts as items hydrate
    Do not emit DocumentNode until the converter can produce partial items.
    Faking page yields from a completed DoclingDocument is not streaming.

    StreamDocument converts sources and streams StreamDocumentResponse events
    (the DocumentStreamEnvelope) until finals are emitted and the stream closes.
    """

    @staticmethod
    def StreamDocument(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_stream(
            request,
            target,
            '/ai.docling.serve.v1.DoclingStreamingService/StreamDocument',
            ai_dot_docling_dot_serve_dot_v1_dot_docling__serve__stream__pb2.StreamDocumentRequest.SerializeToString,
            ai_dot_docling_dot_serve_dot_v1_dot_docling__serve__stream__pb2.StreamDocumentResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
